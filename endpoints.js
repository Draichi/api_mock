// ? GET cover => nomeDaVaga, type, salary, location, description

// ? POST cadastro => email / google / facebook

// ? POST cadastro => nome, cidade, telefone

//  * custom forms

// ? POST CV => file (pdf, doc..) 
// ? POST CV => educational level, experiences... 

// ! ?? Audio questions ??

// ? POST video questions => video file
// ? GET video questions => question

// ? POST OFCCP / DISC / EQ